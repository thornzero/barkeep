import 'package:flutter/material.dart';

class Kitchen extends ChangeNotifier {
  /// Food and Drink Orders

  // final List<Order> _orderQueue = [];
  // UnmodifiableListView<Order> get orderQueue => UnmodifiableListView(_orderQueue);
  // int get orderQueueLength => _orderQueue.length;

  // void addOrderToQueue(Order order) {
  //   _orderQueue.add(order);
  //   notifyListeners();
  // }

  // void removeOrderFromQueue(Order order) {
  //   _orderQueue.remove(order);
  //   notifyListeners();
  // }

  // void clearOrderQueue() {
  //   _orderQueue.clear();
  //   notifyListeners();
  // }
} // BarkeepState
